[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# LP diakses lewat /nama-lp/ (tanpa .html) karena build.js
# menghasilkan folder /<slug>/index.html
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
